{
    "version": 2,
    "buildCommand": "npx prisma generate --schema=./workout-planner/server/prisma/schema.prisma && cd workout-planner/client && npm install && npm run build",
    "outputDirectory": "workout-planner/client/dist",
    "installCommand": "npm install && cd workout-planner && npm install && cd client && npm install && cd ../server && npm install",
    "framework": null,
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "/api/:path*"
        },
        {
            "source": "/((?!api).*)",
            "destination": "/index.html"
        }
    ],
    "functions": {
        "api/**/*.ts": {
            "runtime": "@vercel/node@3"
        }
    }
}